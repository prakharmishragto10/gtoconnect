name: frontend
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: "^3.8.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.2
  http: ^1.2.0
  flutter_map: ^6.1.0
  geolocator: ^12.0.0
  image_picker: ^1.2.1
  http_parser: ^4.1.2
  url_launcher: ^6.3.2
  latlong2: ^0.9.0

dev_dependencies:
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/gto.png"

flutter:
  uses-material-design: true
  assets:
    - assets/gto.png
